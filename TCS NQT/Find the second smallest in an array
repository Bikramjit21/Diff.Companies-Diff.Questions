1st Way: Using sorting
Time Complexity: O(nlogn) Space Complexity: O(1)
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    
    sort(arr,arr+n);
    cout<<arr[1];
}

2nd Way: With the help of second_mini variable
Time Complexity: O(n) Space Complexity: O(1)

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    
    int mini = INT_MAX;
    int second_mini = INT_MAX;
    for(int i=0;i<n;i++){
        mini = min(mini,arr[i]);
    }
    for(int i=0;i<n;i++){
        if(arr[i]<second_mini && arr[i]!=mini){
            second_mini = arr[i];
        }
    }
    cout<<second_mini;
}
