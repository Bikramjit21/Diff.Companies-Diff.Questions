1st Way: Sort the array and then print the increasing array from 0 to (n/2-1)th and then decreasing from (n-1) to (n/2)th.
Time Complexity: 0(nlogn) + O(n).O(nlogn) for sorting and O(n) for printing.

#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	cin>>n;
	
	int arr[n];
	for(int i=0;i<n;i++){
	    cin>>arr[i];
	}
	
	sort(arr,arr+n);
	
	for (int i = 0; i < n / 2; i++) {
		cout << arr[i] << " ";
	}
	for (int i = n - 1; i >= n / 2; i--) {
		cout << arr[i] << " ";
	}
}
