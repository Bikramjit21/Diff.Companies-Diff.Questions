1st Way: Using two for loops
Time Complexity: O(n^2) Space Complexity: O(n)

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    
    vector<bool> v(n,false);
    for(int i=0;i<n;i++){
        if(v[i]==true){
            continue;
        }
        int count=1;
        for(int j=i+1;j<n;j++){
            if(arr[i]==arr[j]){
                v[j]=true;
                count++;
            }
        }
        cout<<arr[i]<<" "<<count<<endl;
    }
}

2nd Way: Using map
Time Complexoty: O(n) Space Complexity: O(n)

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    
    unordered_map<int,int> mp;
    for(auto i:arr){
        mp[arr[i]]++;
    }
    
    for(auto i:mp){
        cout<<i.first<<" "<<i.second<<endl;
    }
}
